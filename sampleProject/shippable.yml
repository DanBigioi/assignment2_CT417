language: java
jdk: oraclejdk8

build:
   ci:
      -mkdir shippable/testresults
      -mkdir shippable/codecoverage
      -shippable_retry mvn install
      -cp -r target shippable/codecoverage